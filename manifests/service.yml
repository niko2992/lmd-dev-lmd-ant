apiVersion: v1
kind: Service
metadata:
    name: "k8stest-8124"
    labels:
        app: "k8stest-8124"
spec:
    type: ClusterIP
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "k8stest-8124"